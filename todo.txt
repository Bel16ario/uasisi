- Add setting to config for domain generation. Add option to automatically generate linspace or polar domain. 

- Code to generate airfoil data

- Code to heal airfoil data from point vector

- Airfoil interpolation (automatic and point-wise using point index (using point index, ))

-For openfoam module, move the nodes, don't remesh every step, could be in the orchestrator init().method. 
    - 

-Change bounds logic in SWVectors to use pairs, minmax

-Check that all range is in bounds in SWVectors when setting bounds or instanciating

-add check() function to SWVector.


